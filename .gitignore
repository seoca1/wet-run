# Build artifacts
site/
.venv/
dist/
build/

# Python
__pycache__/
*.py[cod]
*.pyc
*.egg-info/
*.egg

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Tool session state
.omo/

# Secrets (CRITICAL — never commit)
.env

# Local-only data (not in repo — project data lives under prototype/data/)
/data/

# Local test artifacts (intentional runtime accumulation)
prototype/data/jockeys/deceased.json
prototype/data/saves/*.json

# Cache directories
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local BGM backups (v1 originals — restored on next regeneration)
dashboard/sounds/*.v1_backup.wav
dashboard/sounds/*_v1_orig.wav
